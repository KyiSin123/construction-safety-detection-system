import{Pressable,StyleSheet,Text,View}from'react-native';import{usePathname,useRouter}from'expo-router';
const links=[['/violations','Violations'],['/attendance','Attendance'],['/profile','Profile']]as const;
export function WorkerNav(){const path=usePathname(),router=useRouter();return <View style={s.nav}>{links.map(([href,label])=><Pressable key={href} onPress={()=>router.replace(href)} style={[s.item,path.startsWith(href)&&s.active]}><Text style={[s.text,path.startsWith(href)&&s.activeText]}>{label}</Text></Pressable>)}</View>}const s=StyleSheet.create({nav:{flexDirection:'row',gap:6,marginBottom:14},item:{flex:1,padding:10,borderRadius:6,backgroundColor:'#e9eef5',alignItems:'center'},active:{backgroundColor:'#1769d1'},text:{color:'#43516a',fontWeight:'600'},activeText:{color:'#fff'}});
